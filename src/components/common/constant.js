export const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export const onlyNumber = /^\d+$/;                        // Only number allow nothing else
export const numberRegex = /^[0-9]*\.?[0-9]*$/;   